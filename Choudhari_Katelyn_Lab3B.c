/*
COP3223 Summer 2023 Lab3B
Copyright 2023 Choudhari Katelyn
*/

//Write a program that calculates and prints the sum of the even integers from 0 to 1000, except the number can be divided by 3.

#include <stdio.h>
/*
int main(void) {
	int sum = 0; //init variables

	for (int count = 0; count <= 1000; count += 2) { //init counter

		if (count % 3 == 0) { //checks if a number is divisible by 3
			continue; //if zero remainder, do not add that number to the count
		}
		else {
			sum = sum + count; //sums all the even numbers added to the count
		} //expect those numbers which are divisible by 3
	}
		
		//if (count%3 != 0)
		//{
			//sum = sum + count;
		//}

	printf("%d", sum); //prints the sum 167334

}
*/